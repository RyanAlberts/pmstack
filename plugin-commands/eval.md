---
description: Design an evaluation suite for an AI-powered feature or agentic system
argument-hint: [feature or agent name]
---

Invoke the `pmstack-eval` skill from the pmstack plugin using the Skill tool, passing these arguments verbatim:

$ARGUMENTS

Follow that skill exactly. Do not improvise a lighter-weight version of it. If the skill is unavailable, say so and point the user to /plugin install pmstack@pmstack — do not attempt the task from general knowledge.

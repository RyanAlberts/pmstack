---
description: Check that an AI judge agrees with your labels, then estimate the likely true failure rate
argument-hint: "[project.json] [check id]"
---

Use the Skill tool to run the `pmstack-validate-judge` skill from the pmstack plugin with these arguments: $ARGUMENTS. If the skill is not available, tell the user to run /plugin install pmstack@pmstack.

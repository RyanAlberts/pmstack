---
description: Read real traces, write notes, and group them into failure modes in Eval Studio
argument-hint: "[folder or trace file]"
---

Use the Skill tool to run the `pmstack-error-discovery` skill from the pmstack plugin with these arguments: $ARGUMENTS. If the skill is not available, tell the user to run /plugin install pmstack@pmstack.

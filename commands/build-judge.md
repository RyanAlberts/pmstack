---
description: "Build an AI judge for one failure mode"
argument-hint: "[project.json] [failure mode]"
---

Use the Skill tool to run the `pmstack-build-judge` skill from the pmstack plugin with these arguments: $ARGUMENTS. If the skill is not available, tell the user to run /plugin install pmstack@pmstack.
